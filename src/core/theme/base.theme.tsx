import {
  pallete,
  Button,
  Text,
  InputText,
  Avatar,
  Badge,
  IconButton,
} from './elements';
import { Theme } from './theme';
import { spacing, fade } from './utils/style';

const theme: Theme = {
  pallete,
  components: {
    InputText,
    Button,
    Text,
    Avatar,
    Badge,
    IconButton,
  },
  spacing,
  fade,
};

export default theme;
