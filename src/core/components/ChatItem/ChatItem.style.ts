import styled from 'styled-components/native';

export const ContainerAvatar = styled.View(({}) => ({
  top: 20,
  left: 10,
  position: 'absolute',
}));

export const ContainerName = styled.View(({}) => ({
  position: 'absolute',
  top: 15,
  left: 80,
  right: 45,
}));

export const ContainerTimeMessage = styled.View(({}) => ({
  position: 'absolute',
  top: 20,
  right: 15,
}));

export const ContainerBadge = styled.View(({}) => ({
  position: 'absolute',
  top: 45,
  right: 20,
}));
