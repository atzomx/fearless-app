import styled from 'styled-components/native';

import { Container as CoreContainer } from '@core/ui';

export const Container = styled(CoreContainer)({
  flexDirection: 'row',
  flexWrap: 'wrap',
  justifyContent: 'center',
});
