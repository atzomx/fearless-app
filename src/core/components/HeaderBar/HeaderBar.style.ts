import styled from 'styled-components/native';

export const ContainerAvatar = styled.View(({}) => ({
  top: 15,
  left: 15,
  position: 'absolute',
}));
export const ContainerGreetings = styled.View(({}) => ({
  position: 'absolute',
  top: 15,
  left: 65,
}));
export const ContainerIcon = styled.View(({}) => ({
  position: 'absolute',
  top: 20,
  right: 15,
}));
