const AUTH_ROUTES = {
  base: '@session',
  login: '@session/login',
  start: '@session/start',
  signup: '@session/signup',
  recovery: '@session/recovery',
  code: '@session/code',
  forgot: '@session/forgot',
};

export default AUTH_ROUTES;
