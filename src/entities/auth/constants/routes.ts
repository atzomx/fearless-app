const AUTH_ROUTES = {
  login: '@session/login',
  start: '@session/start',
  signup: '@session/signup',
  recovery: '@session/recovery',
  code: '@session/code',
  forgot: '@session/recovery',
};

export default AUTH_ROUTES;
